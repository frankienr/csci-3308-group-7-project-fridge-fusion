<div class="container-fluid">
<div class="row">
    <!-- Left Panel -->
    <div class="col-md-8 left-panel py-3">
        <div class="card rounded-lg">
            <div class="card-header bg-fridge text-white text-center py-3">
                <h3>My friends</h3>
            </div>
            <ul class="list-group">
                {{#each friends}}
                <!-- this holds the users friends -->
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        {{this.first_name}} {{this.last_name}} ({{this.username}})

                        <form action="/freinds/delete" method="POST">
                            <button class="btn btn-fridge" type="submit">Unfriend</button>
                            <input type="hidden" name="friend" value={{this.username}}></input>
                        </form>                    
                    </li>
                {{/each}}


                <!-- examples to delete later -->
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    Hanna Choi (hannac)
                    <form action="/freinds/delete" method="POST">
                        <button class="btn btn-fridge" type="submit">Unfriend</button>
                        <input type="hidden" name="friend" value={{this.username}}></input>
                    </form>                    
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    Robert Schmidt (rschmidt)
                    <form action="/freinds/delete" method="POST">
                        <button class="btn btn-fridge" type="submit">Unfriend</button>
                        <input type="hidden" name="friend" value={{this.username}}></input>
                    </form>                    
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    Rodrigo Franklin de Mello Nicastro (rodrigodmn)
                    <form action="/freinds/delete" method="POST">
                        <button class="btn btn-fridge" type="submit">Unfriend</button>
                        <input type="hidden" name="friend" value={{this.username}}></input>
                    </form>                    
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    Frankie Nuffer-Rodriguez (frankienr)
                    <form action="/freinds/delete" method="POST">
                        <button class="btn btn-fridge" type="submit">Unfriend</button>
                        <input type="hidden" name="friend" value={{this.username}}></input>
                    </form>                    
                </li>
                
            </ul>
        </div>
        <div class="card rounded-lg mt-2">
            <div class="card-header bg-fridge text-white text-center py-3">
                <h3>Search for more friends</h3>
            </div>
            <div class="card-body">
                <form action="/addfriend" method="POST">
                    <div class="row g-2"> 
                        <div class="col-10">
                            <input name="new_friend" class="form-control" placeholder="Type username" required>
                        </div>
                        <div class="col-2">
                            <button class="btn btn-fridge w-100" type="submit">Add friend</button>
                        </div>
                    </div>
                </form>
            </div>

        </div>
    </div>

    <!-- Right Panel -->
    <div class="col-md-4 right-panel py-3">
        <div class="card rounded-lg overflow-hidden bg-fusion-light">
            <div class="card-header bg-fusion text-white text-center py-3">
                <h3>Find recipes with friends</h3>
            </div>
            <div class="card-body p-4 d-flex justify-content-center">
                <button class="btn btn-danger"><span style="font-family:Apple Chancery" href="/fuse">Fuse</span> with Friends</button>
            </div>
        </div>
    </div>
</div>
</div>